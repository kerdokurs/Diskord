package diskord.server.utils;

import diskord.server.utils.credentials.CredentialError;

public class CredentialVerifier {
  public static CredentialError verify(String data){
//    if(data == null){
//      return CredentialError.NULL_ERROR;
//    }
//
  }
}
